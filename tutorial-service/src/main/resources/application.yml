server:
  port: 9990
spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:mysql://localhost:3306/hr1
    username: root
    password: root
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'